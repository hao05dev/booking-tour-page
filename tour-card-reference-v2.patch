diff --git a/css/style.css b/css/style.css
index 16ab575..debc577 100644
--- a/css/style.css
+++ b/css/style.css
@@ -2902,4 +2902,235 @@ h1, h2, h3, h4, h5, h6 {
   #toursListContainer .tour-card-image-wrap {
     height: 220px;
   }
-}
\ No newline at end of file
+}
+/* ======================================================
+   TOUR LISTING - REFERENCE CARD V2
+   Card layout matched to the supplied reference design.
+   ====================================================== */
+#toursListContainer {
+  grid-template-columns: repeat(2, minmax(0, 1fr));
+  gap: 24px;
+  align-items: stretch;
+}
+
+#toursListContainer .tour-card-reference {
+  display: flex;
+  flex-direction: column;
+  min-width: 0;
+  height: 100%;
+  overflow: hidden;
+  border: 1px solid #d8dee8;
+  border-radius: 20px;
+  background: #ffffff;
+  color: inherit;
+  text-decoration: none;
+  box-shadow: 0 3px 12px rgba(15, 23, 42, 0.11);
+  cursor: pointer;
+  transition: transform 0.22s ease, box-shadow 0.22s ease, border-color 0.22s ease;
+}
+
+#toursListContainer .tour-card-reference:hover {
+  transform: translateY(-4px);
+  border-color: #cbd5e1;
+  box-shadow: 0 14px 30px rgba(15, 23, 42, 0.15);
+}
+
+#toursListContainer .tour-card-reference .tour-card-image-wrap {
+  position: relative;
+  width: 100%;
+  height: auto;
+  aspect-ratio: 16 / 10;
+  overflow: hidden;
+  flex-shrink: 0;
+  border-radius: 0;
+  background: #e2e8f0;
+}
+
+#toursListContainer .tour-card-reference .tour-card-img {
+  display: block;
+  width: 100%;
+  height: 100%;
+  object-fit: cover;
+  transition: transform 0.45s ease;
+}
+
+#toursListContainer .tour-card-reference:hover .tour-card-img {
+  transform: scale(1.035);
+}
+
+#toursListContainer .tour-promo-badge {
+  position: absolute;
+  top: 18px;
+  left: 0;
+  z-index: 2;
+  display: inline-flex;
+  align-items: center;
+  min-height: 34px;
+  padding: 7px 13px;
+  border-radius: 0 7px 7px 0;
+  background: #ef4444;
+  color: #ffffff;
+  font-size: 0.82rem;
+  font-weight: 800;
+  line-height: 1;
+  box-shadow: 0 2px 7px rgba(15, 23, 42, 0.18);
+}
+
+#toursListContainer .tour-card-reference .tour-card-body {
+  display: flex;
+  flex: 1;
+  flex-direction: column;
+  min-width: 0;
+  padding: 18px 18px 16px;
+}
+
+#toursListContainer .tour-rating-row {
+  display: flex;
+  align-items: center;
+  gap: 8px;
+  min-height: 32px;
+  margin-bottom: 14px;
+}
+
+#toursListContainer .tour-rating-score {
+  display: inline-flex;
+  align-items: center;
+  justify-content: center;
+  min-width: 54px;
+  height: 31px;
+  padding: 0 10px;
+  border-radius: 9px;
+  background: #34a853;
+  color: #ffffff;
+  font-size: 1rem;
+  font-weight: 800;
+  line-height: 1;
+}
+
+#toursListContainer .tour-rating-label {
+  color: #2f9e44;
+  font-size: 0.95rem;
+  font-weight: 700;
+}
+
+#toursListContainer .tour-rating-count {
+  color: #64748b;
+  font-size: 0.92rem;
+}
+
+#toursListContainer .tour-card-reference .tour-title {
+  display: -webkit-box;
+  min-height: calc(1.42em * 2);
+  margin: 0 0 16px;
+  overflow: hidden;
+  color: #1e293b;
+  font-size: 1.17rem;
+  font-weight: 700;
+  line-height: 1.42;
+  -webkit-box-orient: vertical;
+  -webkit-line-clamp: 2;
+}
+
+#toursListContainer .tour-info-row {
+  display: flex;
+  align-items: center;
+  flex-wrap: wrap;
+  gap: 10px 18px;
+  margin-bottom: 16px;
+  color: #64748b;
+  font-size: 0.88rem;
+}
+
+#toursListContainer .tour-info-row span {
+  display: inline-flex;
+  align-items: center;
+  gap: 6px;
+  min-width: 0;
+}
+
+#toursListContainer .tour-info-row i {
+  color: #8b95a5;
+}
+
+#toursListContainer .tour-tag-row {
+  display: grid;
+  grid-template-columns: repeat(2, minmax(0, 1fr));
+  gap: 10px;
+  margin-bottom: 18px;
+}
+
+#toursListContainer .tour-tag {
+  display: block;
+  min-width: 0;
+  padding: 8px 10px;
+  overflow: hidden;
+  border: 1px solid #e2e8f0;
+  border-radius: 8px;
+  background: #f8fafc;
+  color: #334155;
+  font-size: 0.86rem;
+  line-height: 1.2;
+  text-align: center;
+  text-overflow: ellipsis;
+  white-space: nowrap;
+}
+
+#toursListContainer .tour-card-footer-reference {
+  display: flex;
+  justify-content: flex-end;
+  align-items: flex-end;
+  margin-top: auto;
+  padding-top: 16px;
+  border-top: 1px solid #e2e8f0;
+}
+
+#toursListContainer .tour-price-reference {
+  width: 100%;
+  text-align: right;
+}
+
+#toursListContainer .tour-price-line {
+  display: flex;
+  align-items: baseline;
+  justify-content: flex-end;
+  flex-wrap: wrap;
+  gap: 8px;
+  color: #64748b;
+  font-size: 0.91rem;
+}
+
+#toursListContainer .tour-price-line strong {
+  color: #f97316;
+  font-size: 1.28rem;
+  font-weight: 800;
+  line-height: 1.2;
+  white-space: nowrap;
+}
+
+#toursListContainer .tour-departure-date {
+  margin-top: 4px;
+  color: #64748b;
+  font-size: 0.86rem;
+}
+
+@media (max-width: 900px) {
+  #toursListContainer {
+    grid-template-columns: 1fr;
+  }
+}
+
+@media (max-width: 600px) {
+  #toursListContainer .tour-card-reference .tour-card-image-wrap {
+    aspect-ratio: 16 / 10;
+    height: auto;
+  }
+
+  #toursListContainer .tour-card-reference .tour-title {
+    min-height: 0;
+    font-size: 1.08rem;
+  }
+
+  #toursListContainer .tour-tag-row {
+    grid-template-columns: 1fr 1fr;
+  }
+}
diff --git a/js/main.js b/js/main.js
index e8b84ca..cfccf62 100644
--- a/js/main.js
+++ b/js/main.js
@@ -382,6 +382,44 @@ function initToursPage() {
   const db = getMockDatabase();
   let allTours = [...db.tours];
 
+  const regionLabels = {
+    north: 'Miền Bắc',
+    central: 'Miền Trung',
+    south: 'Miền Nam'
+  };
+
+  const themeLabels = {
+    sea: 'Biển đảo',
+    nature: 'Thiên nhiên',
+    culture: 'Văn hóa'
+  };
+
+  function parseVnDate(value) {
+    if (!value) return Number.MAX_SAFE_INTEGER;
+    const [day, month, year] = value.split('/').map(Number);
+    return new Date(year, month - 1, day).getTime();
+  }
+
+  function getNextDeparture(tourId) {
+    return db.departures
+      .filter(dep => dep.tourId === tourId && dep.status !== 'Sold Out')
+      .sort((a, b) => parseVnDate(a.date) - parseVnDate(b.date))[0] || null;
+  }
+
+  function getRatingLabel(rating) {
+    if (rating >= 4.8) return 'Tuyệt vời';
+    if (rating >= 4.6) return 'Xuất sắc';
+    if (rating >= 4.4) return 'Rất tốt';
+    return 'Tốt';
+  }
+
+  function getPromoLabel(tour) {
+    if (tour.featured) return 'Giá ưu đãi';
+    if (tour.popular) return 'Tour bán chạy';
+    if (tour.topRated) return 'Được yêu thích';
+    return '';
+  }
+
   function filterAndRenderTours() {
     const keyword = inputSearch ? inputSearch.value.trim().toLowerCase() : searchKeyword.toLowerCase();
     const currentRegion = document.querySelector('.tour-category-tab.active')?.dataset.region || 'all';
@@ -467,37 +505,54 @@ function initToursPage() {
       return;
     }
 
-    toursGrid.innerHTML = filtered.map(tour => `
-      <article class="tour-card">
-        <div class="tour-card-image-wrap">
-          <img src="${tour.image}" alt="${tour.title}" class="tour-card-img" loading="lazy">
-          <div class="tour-card-badges">
-            ${tour.featured ? '<span class="badge badge-warning"><i class="fa-solid fa-fire"></i> Nổi bật</span>' : ''}
-            <span class="badge badge-neutral"><i class="fa-solid fa-location-dot"></i> ${tour.location}</span>
+    toursGrid.innerHTML = filtered.map(tour => {
+      const nextDeparture = getNextDeparture(tour.id);
+      const promoLabel = getPromoLabel(tour);
+      const ratingScore = (tour.rating * 2).toFixed(1);
+      const regionLabel = regionLabels[tour.region] || tour.location;
+      const themeLabel = themeLabels[tour.theme] || 'Khám phá';
+
+      return `
+        <a class="tour-card tour-card-reference" href="tour-detail.html?id=${tour.id}" aria-label="Xem chi tiết ${tour.title}">
+          <div class="tour-card-image-wrap">
+            <img src="${tour.image}" alt="${tour.title}" class="tour-card-img" loading="lazy">
+            ${promoLabel ? `<span class="tour-promo-badge">${promoLabel}</span>` : ''}
           </div>
-          <button class="tour-wishlist-btn" aria-label="Yêu thích" onclick="toggleWishlist(this, '${tour.id}')">
-            <i class="fa-regular fa-heart"></i>
-          </button>
-        </div>
-        <div class="tour-card-body">
-          <div class="tour-meta">
-            <span class="tour-duration"><i class="fa-regular fa-clock"></i> ${tour.duration}</span>
-            <span class="tour-rating"><i class="fa-solid fa-star text-warning"></i> <strong>${tour.rating}</strong> (${tour.reviewsCount})</span>
-          </div>
-          <h3 class="tour-title">
-            <a href="tour-detail.html?id=${tour.id}">${tour.title}</a>
-          </h3>
-          <p class="tour-description">${tour.description}</p>
-          <div class="tour-card-footer">
-            <div class="tour-price-box">
-              <span class="tour-price-label">Giá từ</span>
-              <span class="tour-price-val">${formatCurrency(tour.price)}</span>
+
+          <div class="tour-card-body">
+            <div class="tour-rating-row">
+              <span class="tour-rating-score">${ratingScore}</span>
+              <strong class="tour-rating-label">${getRatingLabel(tour.rating)}</strong>
+              <span class="tour-rating-count">(${tour.reviewsCount})</span>
+            </div>
+
+            <h3 class="tour-title">${tour.title}</h3>
+
+            <div class="tour-info-row">
+              <span><i class="fa-regular fa-clock"></i> ${tour.days} ngày</span>
+              <span><i class="fa-solid fa-location-dot"></i> Điểm đến ${tour.location}</span>
+            </div>
+
+            <div class="tour-tag-row">
+              <span class="tour-tag">${themeLabel}</span>
+              <span class="tour-tag">${regionLabel}</span>
+            </div>
+
+            <div class="tour-card-footer-reference">
+              <div class="tour-price-reference">
+                <div class="tour-price-line">
+                  <span>Giá chỉ</span>
+                  <strong>${formatCurrency(tour.price)}</strong>
+                </div>
+                <div class="tour-departure-date">
+                  ${nextDeparture ? `Khởi hành ngày ${nextDeparture.date.slice(0, 5)}` : 'Liên hệ lịch khởi hành'}
+                </div>
+              </div>
             </div>
-            <a href="tour-detail.html?id=${tour.id}" class="btn btn-outline btn-sm">Chi tiết <i class="fa-solid fa-arrow-right"></i></a>
           </div>
-        </div>
-      </article>
-    `).join('');
+        </a>
+      `;
+    }).join('');
   }
 
   window.clearSearchKeyword = () => {
